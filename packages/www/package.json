{
  "name": "www",
  "type": "module",
  "scripts": {
    "dev": "fastly compute serve --watch",
    "prebuild": "bun ./build.config.ts && bun ./toml.ts",
    "build": "js-compute-runtime bin/index.js bin/main.wasm",
    "deploy": "fastly compute publish",
    "vite": "bunx --bun vite"
  },
  "devDependencies": {
    "@fastly/js-compute": "^2.4.0",
    "@hono/vite-dev-server": "^0.0.8",
    "vite": "^4.4.9"
  },
  "dependencies": {
    "build-toml": "workspace:*",
    "esbuild-plugin-polyfill-node": "^0.3.0",
    "hono-local": "workspace:*",
    "db": "workspace:*",
    "fastly-plugin": "workspace:*"
  }
}
